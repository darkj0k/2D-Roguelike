using UnityEngine;

public class Movement : MonoBehaviour
{
    private void Move()
    {
        while (true)
        {
            UnityEngine.Input.
        }
    }
    // Update is called once per frame
    void Start()
    {
        Move();
    }
}
